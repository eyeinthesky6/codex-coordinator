interface:
  display_name: "Codex Coordinator"
  short_description: "Keep parallel Codex tasks from crossing wires"
  default_prompt: "Use $codex-coordinator to show or claim the bounded work for this task in the current repository."
