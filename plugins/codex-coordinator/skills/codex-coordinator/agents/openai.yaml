interface:
  display_name: "Codex Coordinator"
  short_description: "Keep parallel Codex tasks from crossing wires"
  default_prompt: "Use $codex-coordinator to coordinate this goal in complete bounded task lanes on the current shared checkout, without background monitoring."
