interface:
  display_name: "Codex Coordinator"
  short_description: "Keep Codex tasks on track"
  default_prompt: "Use $codex-coordinator to coordinate this project goal across the right existing tasks and keep each task focused."
