interface:
  display_name: "Codex Coordinator"
  short_description: "Give Codex tasks clear jobs"
  default_prompt: "Use $codex-coordinator to reuse the right existing tasks for this goal, give each one a clear job, and show where work may cross."
